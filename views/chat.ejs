<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat: <%= groupName %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --whatsapp-bg: #efe7dd;
            --support-msg: #dcf8c6;
            --customer-msg: #ffffff;
            --header-color: #075e54;
        }

        body { 
            font-family: 'Inter', sans-serif; 
            margin: 0; 
            display: flex; 
            flex-direction: column; 
            height: 100vh; 
            background: var(--whatsapp-bg);
        }

        /* Header Styling */
        header {
            background: var(--header-color);
            color: white;
            padding: 15px 20px;
            display: flex;
            align-items: center;
            gap: 15px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            z-index: 10;
        }

        header a { color: white; text-decoration: none; font-size: 1.2rem; }
        header h1 { margin: 0; font-size: 1.1rem; flex-grow: 1; }

        /* Chat Area */
        .chat-container {
            flex-grow: 1;
            overflow-y: auto;
            padding: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .msg { 
            padding: 8px 12px; 
            border-radius: 8px; 
            max-width: 75%; 
            position: relative;
            box-shadow: 0 1px 2px rgba(0,0,0,0.1);
            line-height: 1.4;
        }

        .support { 
            background: var(--support-msg); 
            align-self: flex-end; 
            border-top-right-radius: 0;
        }

        .customer { 
            background: var(--customer-msg); 
            align-self: flex-start; 
            border-top-left-radius: 0;
        }

        .sender-name {
            display: block;
            font-size: 0.75rem;
            font-weight: bold;
            color: #128c7e;
            margin-bottom: 3px;
        }

        .msg p { margin: 0; font-size: 0.95rem; color: #333; }

        .time {
            display: block;
            text-align: right;
            font-size: 0.7rem;
            color: #888;
            margin-top: 4px;
        }

        /* Reply Box */
        .reply-box {
            background: #f0f0f0;
            padding: 15px;
            display: flex;
            gap: 10px;
            align-items: center;
            border-top: 1px solid #ddd;
        }

        .reply-box input {
            flex-grow: 1;
            padding: 12px;
            border-radius: 25px;
            border: 1px solid #ccc;
            outline: none;
            font-size: 0.9rem;
        }

        .send-btn {
            background: #128c7e;
            color: white;
            border: none;
            width: 45px;
            height: 45px;
            border-radius: 50%;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: transform 0.2s;
        }

        .send-btn:hover { transform: scale(1.05); }
    </style>
</head>
<body>

    <header>
        <a href="/dashboard"><i class="fas fa-arrow-left"></i></a>
        <h1><%= groupName %></h1>
        <i class="fas fa-ellipsis-v"></i>
    </header>

    <div class="chat-container" id="chatBox">
        <% messages.forEach(msg => { %>
            <div class="msg <%=numbers.includes(msg.person_num)? 'support' :'customer' %>">
             

                <span class="sender-name">
                    <%= numbers.includes(msg.person_num)? 'Support' : 'Customer' %>
                </span>
                <p><%= msg.message_text %></p>
                <span class="time"><%= new Date(msg.milli_sec).toString() %></span>
            </div>
        <% }) %>
    </div>

   

    <script>
        // Scroll to bottom on load
        const chatBox = document.getElementById('chatBox');
        chatBox.scrollTop = chatBox.scrollHeight;

        // async function sendReply() {
        //     const text = document.getElementById('replyText').value;
        //     if(!text) return;

        //     // Assuming groupName can be used to find the JID on the backend
        //     // Or pass groupJID into the template if you have it
        //     const response = await fetch('/send', {
        //         method: 'POST',
        //         headers: { 'Content-Type': 'application/json' },
        //         body: JSON.stringify({
        //             to: "<%= groupName %>", // Note: This needs the @g.us ID usually, see below
        //             text: text
        //         })
        //     });

        //     if(response.ok) {
        //         location.reload(); // Refresh to show the new message
        //     } else {
        //         alert("Failed to send message. Make sure the 'to' field is a valid JID.");
        //     }
        // }
    </script>
</body>
</html>